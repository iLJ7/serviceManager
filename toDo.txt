1. Prevent user from opening multiple Toplevel windows (e.g. can't have 2 add service/change driver windows). if not newWindow, drivWindow etc may work.
2. Pull driver info from drivers file, not data.txt
3. Style.